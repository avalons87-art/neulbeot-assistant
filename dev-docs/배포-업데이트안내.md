# 자동 업데이트 설정·배포하기 (신현종 선생님용)

교사 설치본은 **git 없이** 최신화됩니다. 앱의 **🔃 업데이트** 버튼(또는 `update.bat`)이 GitHub에서 최신 코드 zip을 내려받아 덮어써요. 각자의 키·폴더·일정 설정은 그대로 유지됩니다(덮어쓰기 대상에서 제외).

> **선생님(교사)들은 git을 깔 필요가 없어요.** Node.js만 있으면 됩니다.
> git은 **신현종 선생님(배포자)** 이 저장소에 코드를 올릴 때만 씁니다.

## 처음 한 번 — 저장소 만들기 (배포자만)

> ⚠️ 저장소엔 **코드만** 올라가요. 키(`my-keys.bat`, `keys.json`)·개인설정은 `.gitignore`로 빠져서 **절대 안 올라갑니다.** 그래서 저장소는 **공개(public)**로 둬도 안전하고, 앱이 인증 없이 받을 수 있어요.

1. GitHub에서 **새 공개 저장소**(예: `neulbeot-assistant`) 생성
2. 이 폴더에서 (git 설치 필요: git-scm.com):
   ```
   git init
   git add .
   git commit -m "first"
   git branch -M main
   git remote add origin https://github.com/내계정/neulbeot-assistant.git
   git push -u origin main
   ```
3. **`update-source.txt`** 파일을 열어 `owner/repo` 를 실제 주소로 바꿔요:
   ```
   내계정/neulbeot-assistant
   ```
   (다른 브랜치면 `내계정/neulbeot-assistant@브랜치명`)
4. 확인: 커밋 후 GitHub에 `my-keys.bat`, `keys.json` 등이 **안 보이면** 정상.

## 배포

- `teacher-keys.bat`에 공유(한도) 키 넣기 → `make-teacher-package.bat` 실행 → zip 생성
- zip 안에는 `.git`이 **빠져** 있어요(교사는 git 불필요). 대신 `update-source.txt`가 들어가서, 앱이 그 주소에서 업데이트를 받아요.
- 선생님: zip 풀고 `start-server.bat` 실행 → 자기 폴더 선택

## 업데이트 (코드 바꾼 뒤)

1. 신현종 선생님이 코드 수정 → `package.json`의 `version`을 올리고(예: 1.0 → 1.1) → `git add . && git commit -m "..." && git push`
   - **버전 숫자를 꼭 올려야** 교사 앱이 "새 버전 있음"으로 인식해요.
2. 각 선생님은 다음 중 하나로 최신화 (키·폴더·일정 유지됨):
   - 앱 상단 **🔃 업데이트** 버튼 → 새 버전이 있으면 "업데이트 있음"으로 표시 → 누르면 자동으로 받고 재시작 → 10초 뒤 F5 새로고침
   - 또는 앱 끄고 **`update.bat`** 실행 → 다시 `start-server.bat`

## 주요(필수) 업데이트를 강제로 적용하기

교사가 🔃 버튼을 안 눌러도, **켤 때/새로고침할 때 자동으로** 업데이트되게 하려면:

1. 코드 수정 + `package.json`의 `version` 올리기
2. `package.json`의 **`"forceUpdate": true`** 로 바꾸기
3. `git add . && git commit && git push`

→ 교사 앱이 다음에 열릴 때(또는 새로고침 시) **버튼 없이 자동으로 받고 재시작**해요. (`start-server.bat`의 재시작 루프가 새 코드로 다시 띄움)

- **평소 업데이트는 `"forceUpdate": false`** 로 두세요(교사가 🔃 버튼으로 선택 적용). 주요 업데이트일 때만 `true`.
- 이 강제 기능은 **교사가 이미 이 기능이 든 버전(0.8.0 이상)을 받은 뒤부터** 작동해요. 그 전 버전 교사는 한 번은 🔃로 받아야 해요.
- 강제 적용은 코드만 자동 교체(키·폴더·일정 등 개인설정은 그대로).

## 참고

- `update-source.txt`가 `owner/repo` 그대로면(미설정) 🔃 버튼이 **숨겨져요**. 주소를 넣어야 보입니다.
- 업데이트는 **코드 파일만** 덮어써요. `my-keys.bat`, `keys.json`, `owner.txt`, `work-dir.txt`, `brand.txt`, `schedule.json`, 폴더·분석 설정, `update-source.txt`, 그리고 `outputs/`·`node_modules/`는 **건드리지 않습니다.**
- 의존성(package.json)이 바뀌면 업데이트 시 자동으로 `npm install`도 돌아요.
- 키 파일이 실수로 커밋됐다면: `git rm --cached my-keys.bat keys.json` 후 다시 커밋하고, **그 키는 폐기(재발급)** 하세요.
